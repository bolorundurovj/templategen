{
  "name": "<%= projectName %>",
  "version": "1.0.0",
  "description": "Backend API scaffolded with TemplateGen",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{js,json}\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"<% if (database === 'mongodb') { %>,
    "mongoose": "^7.4.1"<% } %><% if (database === 'postgresql') { %>,
    "pg": "^8.11.3"<% } %><% if (database === 'mysql') { %>,
    "mysql2": "^3.6.0"<% } %><% if (database === 'sqlite') { %>,
    "sqlite3": "^5.1.6"<% } %>
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  }
}
