enum ProjectTypes{
    FRONTEND = 'frontend',
    BACKEND = 'backend',
    FULLSTACK = 'fullstack'
}

const PROJECT_TYPES = [
    ProjectTypes.FRONTEND,
    ProjectTypes.BACKEND,
    ProjectTypes.FULLSTACK
]

export { ProjectTypes, PROJECT_TYPES };
